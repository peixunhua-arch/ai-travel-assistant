# 使用国内镜像，避免 npmjs.org 下载超时（尤其是 esbuild 等原生二进制）
registry=https://registry.npmmirror.com

# Expo/React Native 在 pnpm 下要求扁平化 node_modules，否则 Metro 打包会报
# "Unable to resolve module"。这是 Expo 官方推荐设置。
node-linker=hoisted
